#include <stdio.h>

/**
 * main - Entry point of the program
 * @argc: The number of arguments (unused in this program)
 * @argv: An array of pointers to the arguments (unused in this program)
 *
 * Return: Always 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

